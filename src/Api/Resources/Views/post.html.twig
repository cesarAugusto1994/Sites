{% extends 'template.html.twig' %}

{% block title %}{{ post.titulo }}{% endblock %}

{% block content %}

    <div class="blog-post">
        <h2 class="blog-post-title">{{ post.titulo }}</h2>
        <small class="blog-post-meta">{{ post.cadastro|date('F j, Y') }} by <a href="{{ app.url_generator.generate('author', { author : post.usuario.id, name : post.usuario.nome|replace(' ', '_')|lower }) }}">{{ post.usuario.nome }}</a></small>
        <p>{{ post.descricao|raw }}</p>
    </div>

    <table class="table table-bordered table-responsive">
        <tbody>
        {% for link in links %}
            <tr>
                <td>
                    {% if link.media == 'audio' %}
                        <p class="lead">{{ link.titulo }}</p>
                        <iframe width="100%" height="166" scrolling="no" frameborder="no"
                                src="{{ link.url }}&amp;color=ff5500&amp;auto_play=false&amp;hide_related=false&amp;show_comments=true&amp;show_user=true&amp;show_reposts=false"></iframe>
                    {% else %}
                        <p class="lead"><a target="_blank" href="{{ link.url }}">{{ link.titulo }}</a></p>
                    {% endif %}
                </td>
            </tr>
        {% else %}
            <tr>
                <td>Sem anexos.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <!--
    <div class="col-md-12">
        <div class="nav-tabs-custom">
            <ul class="nav nav-tabs">
                <li class="active"><a href="#activity" data-toggle="tab">Vozes</a></li>
                <li><a href="#timeline" data-toggle="tab">Instrumentos</a></li>
                <li><a href="#settings" data-toggle="tab">Settings</a></li>
            </ul>
            <div class="tab-content">
                <div class="active tab-pane" id="activity">
                    <table class="table">
                        <tbody>
                        {% for link in links %}
                            {% if link.media == 'audio' %}
                            <tr>
                                <td>
                                    <p class="lead">{{ link.titulo }}</p>
                                    <iframe width="100%" height="166" scrolling="no" frameborder="no" src="{{ link.url }}&amp;color=ff5500&amp;auto_play=false&amp;hide_related=false&amp;show_comments=true&amp;show_user=true&amp;show_reposts=false"></iframe>
                                </td>
                            </tr>
                            {% endif %}
                        {% else %}
                            <tr>
                                <td>Sem anexos.</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
                <div class="tab-pane" id="timeline">
                    <table class="table">
                        <tbody>
                        {% for link in links %}
                            {% if link.media == 'PDF' %}
                                <tr>
                                    <td>
                                        <p class="lead"><a target="_blank" href="{{ link.url }}">{{ link.titulo }}</a></p>
                                    </td>
                                </tr>
                            {% endif %}
                        {% else %}
                            <tr>
                                <td>Sem anexos.</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
                <div class="tab-pane" id="settings">
                    <table class="table">
                        <tbody>
                        {% for link in links %}
                            {% if link.media == 'video' %}
                                <tr>
                                    <td>
                                        <p class="lead">{{ link.titulo }}</p>
                                        <iframe width="100%" height="166" scrolling="no" frameborder="no" src="{{ link.url }}&amp;color=ff5500&amp;auto_play=false&amp;hide_related=false&amp;show_comments=true&amp;show_user=true&amp;show_reposts=false"></iframe>
                                    </td>
                                </tr>
                            {% endif %}
                        {% else %}
                            <tr>
                                <td>Sem anexos.</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div> -->

    <div id="disqus_thread"></div>

    <script id="dsq-count-scr" src="//site-14.disqus.com/count.js" async></script>

{% endblock %}